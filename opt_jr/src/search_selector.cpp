#include "search_selector.hh"

/*
Search Search_selector::select(optJrParameters par)
{
  if (par.get_search_type()=="a" | par.get_search_type()=="A")
  {
    return Search<Search_alterning>;
  }

  else if(par.get_search_type()=="s" | par.get_search_type()=="S")
  {
    return Search<Search_separing>;
  }
  else{
    std::cout<< "FATAL ERROR: unknown search_type "<<std::endl;
    exit(-1)
  }


}
*/
